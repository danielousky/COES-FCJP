#area_field.control-group.row.mb-3.enum_type
  %label.col-sm-2.col-form-label.text-md-end{for: "area_field"} Cátedra
  .col-sm-10.controls
    = grouped_collection_select nil, :area_id, Area.all.joins(:school).order('areas.name': :asc, 'schools.name': :asc), :areas, :full_description, :id, :name, {prompt: "Seleccione Cátedra..."}, { class: "form-control"}
    .form-text
      %span.label.label-danger Requerida
